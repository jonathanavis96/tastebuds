# pi-deploy config for TasteBuds.  Usage:  pi-deploy ~/code/tastebuds
# Source of truth = this repo; deploys to pihome and rebuilds the container.
PI_HOST=pihome
DEST=/home/grafe/stacks/tastebuds
APPLY=compose
COMPOSE_ARGS="up -d --build"
VERIFY_URL=http://localhost:8094
